(* 4.1 *)
let baito_kyuyo h y = h * (850 + (100 * y))

(* 4.2 *)
let jikoshokai name = "私の名前は" ^ name ^ "です"

(* 4.3 *)
let hyojun_taiju m = (m ** 2.) *. 22.

(* 4.4 *)
let bmi m kg = kg /. (m ** 2.)
